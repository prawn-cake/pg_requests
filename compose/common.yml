postgresql_common:
  build: ../pg_query/system_test
  environment:
    POSTGRES_PASSWORD: test # Set password for default (postgres) user
    PGPASSWORD: test        # For using `psql -c ...` without password